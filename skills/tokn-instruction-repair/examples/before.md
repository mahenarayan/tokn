# Repository Instructions

- Follow best practices and write clean code.
- Use TypeScript strict mode where possible and try to keep types explicit for public APIs because it helps code review and maintainability across packages.

