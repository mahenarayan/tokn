# Repair Tokn Instruction Findings

Use the canonical workflow in `skills/tokn-instruction-repair/skill.md`.

Repair Tokn `instructions-lint` findings by preserving instruction intent,
making guidance crisp and scoped, resolving duplicates or conflicts, and
running the configured Tokn verification command after edits.

Do not remove important requirements only to reduce token count. Optimize
signal first.
